<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="myproject.dao.QiandaoqiantuiDao">

    <resultMap type="myproject.entity.QiandaoqiantuiEntity" id="qiandaoqiantuiMap">
        <result property="id" column="id"/>
        <result property="userid" column="userid"/>
        <result property="username" column="username"/>
        <result property="status" column="status"/>
        <result property="qiandaotime" column="qiandaotime"/>
        <result property="qiantuitime" column="qiantuitime"/>
        <result property="addtime" column="addtime"/>
    </resultMap>
        <select id="selectValue" resultType="java.util.Map">
        SELECT ${params.xColumn}, count(${params.yColumn}) total FROM qiandaoqiantui
        <where>${ew.sqlSegment}</where>
        group by ${params.xColumn}
        limit 10
    </select>
        <select id="selectTimeStatValue" resultType="java.util.Map">
        <if test='params.timeStatType == "日"'>
            SELECT DATE_FORMAT(${params.xColumn},'%Y-%m-%d') ${params.xColumn}, count(1) qiandaoqiantui FROM
            qiandaoqiantui
            <where>${ew.sqlSegment}</where>
            group by DATE_FORMAT(${params.xColumn},'%Y-%m-%d')
        </if>
        <if test='params.timeStatType == "月"'>
            SELECT DATE_FORMAT(${params.xColumn},'%Y-%m') ${params.xColumn}, count(1) qiandaoqiantui  FROM
            qiandaoqiantui
            <where>${ew.sqlSegment}</where>
            group by DATE_FORMAT(${params.xColumn},'%Y-%m')
        </if>
        <if test='params.timeStatType == "年"'>
            SELECT DATE_FORMAT(${params.xColumn},'%Y') ${params.xColumn}, count(1) qiandaoqiantui FROM
            qiandaoqiantui
            <where>${ew.sqlSegment}</where>
            group by DATE_FORMAT(${params.xColumn},'%Y')
        </if>
        order by ${params.xColumn}
    </select>
        <select id="selectGroup" resultType="java.util.Map">
        SELECT ${params.column} , count(1) qiandaoqiantui  FROM qiandaoqiantui
        <where>${ew.sqlSegment}</where>
        group by ${params.column}
        limit 10
    </select>
</mapper>