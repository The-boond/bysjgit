let userPath = 'user' // 用户路径
let commentPath = 'discuss' // 评论路径
let newsPath = 'news' // 公告
let aboutusPath = 'aboutus' // 关于我们
let configPath = 'config' // 配置



export {
    userPath,
    commentPath,
    newsPath,
    aboutusPath,
    configPath,
}